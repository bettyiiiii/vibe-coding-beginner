{
  "version": 2,
  "buildCommand": "npm run build",
  "build": {
    "env": {
      "NODE_VERSION": "18"
    }
  },
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
